#pragma once

#include <string>
#include <vector>
#include <map>

class ConfigParser {
		private:
				std::string _configValue;
				size_t 		_pos;

				
		public:
				ConfigParser();
				~ConfigParser();

};